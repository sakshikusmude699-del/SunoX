# Copy to keystore.properties (do not commit) and fill in for Play upload signing.
# Generate an upload keystore: https://developer.android.com/studio/publish/app-signing#generate-key

storeFile=release-keystore.jks
storePassword=change-me
keyAlias=upload
keyPassword=change-me
